#include "main.h"

/**
 * add - Adds two numbers
 * @int: The integer input
 *
 * Return: Returns the result of of integer inputs added.
 */
int add(int, int);

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
